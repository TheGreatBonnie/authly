# CLI

The initial CLI provides a small command surface.

Authenticate a local project:

```bash
authly login --project proj_demo
```

List users:

```bash
authly user list
```

The CLI is intentionally minimal in version 0.1.
