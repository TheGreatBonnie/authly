# API keys

The initial SDK requires an API key when the client is initialized.

```python
authly = Authly(
    project_id="proj_demo",
    api_key="demo_key",
)
```

API keys are part of the fictional product's initial authentication model.

Future simulation scenarios intentionally change this behavior.
